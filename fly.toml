# fly.toml app configuration file generated for nongor-bot on 2026-02-10T23:52:37+06:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'nongor-bot'
primary_region = 'sin'

[build]
  dockerfile = 'Dockerfile'

[env]
  PORT = '8080'

# [[services]] block removed for background worker mode
# The bot uses polling and does not need to listen on a port.

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
